{{#if @title}}
    <SetListTitle @title={{@title}} @songs={{@songs}} />
{{/if}}

<EuiFlexGrid @columns={{1}}>
    {{#each @songs as |song|}}
        <EuiFlexItem>
            <Song @user={{@user}} @song={{song}} />
        </EuiFlexItem>
    {{/each}}
</EuiFlexGrid>