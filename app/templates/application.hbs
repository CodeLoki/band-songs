<EuiHeader @position="fixed">
    <EuiHeaderSectionItem @border="right">
        <EuiHeaderLogo {{on "click" this.authenticate}}>Band Songs</EuiHeaderLogo>
    </EuiHeaderSectionItem>

    <EuiHeaderSectionItem @border="none">
        <EuiHeaderLinks>
            <EuiHeaderSectionItem @border="right">
                <div id="header-links" />
            </EuiHeaderSectionItem>

            <EuiHeaderLink
                {{on "click" (go-to "index")}}
                @isSelected={{eq "/" this.router.currentRouteName}}
                @iconType="graphApp"
            >
                Gigs
            </EuiHeaderLink>

            <EuiHeaderLink
                @isSelected={{eq "/songs" this.router.currentRouteName}}
                @iconType="discoverApp"
                {{on "click" (go-to "songs")}}
            >
                Songs
            </EuiHeaderLink>
        </EuiHeaderLinks>
    </EuiHeaderSectionItem>
</EuiHeader>

<main>
    {{outlet}}
</main>

{{page-title "Band Songs"}}

{{! @glint-expect-error - `toasts` seems to be required }}
<EuiGlobalToastList @toastLifeTimeMs={{4500}} />