{{page-title "Songs"}}

{{#in-element (get-header-links)}}
    {{#if (can-user-edit)}}
        <EuiHeaderLink
            @iconType="createAdvancedJob"
            {{on "click" (go-to "songs.edit" "new")}}
        >
            Add
        </EuiHeaderLink>
    {{/if}}
{{/in-element}}

{{outlet}}